# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Environment and secrets
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets/

# Test and coverage
coverage/
.nyc_output/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editors and OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local tooling
.claude/settings.local.json

# Browser tests
playwright-report/
test-results/

# Written by scripts/deploy.sh into a temporary directory, never here. Ignored
# in case the runbook is followed by hand from the repository.
Dockerfile.vercel
vercel.json
.vercel/

# The design run's working record. The surface files are kept — they hold the
# direction contract DESIGN.md was derived from. The review rasters and the
# question log are not: both regenerate from the app, and the rasters are large.
web/.impeccable/review/
web/.impeccable/questions/
