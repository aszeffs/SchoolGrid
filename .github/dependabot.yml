version: 2

updates:
  # Application dependencies. Grouping minor and patch into one pull request
  # keeps staying current from becoming a queue of trivial reviews; majors are
  # raised individually because those are the ones worth reading.
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Asia/Manila
    open-pull-requests-limit: 5
    commit-message:
      prefix: chore
      prefix-development: chore
      include: scope
    groups:
      npm-minor-and-patch:
        applies-to: version-updates
        patterns: ["*"]
        update-types: [minor, patch]

  # Actions are pinned to commit SHAs, which is what stops a moved tag from
  # changing what runs here. Without this, pinning would decay into running
  # permanently outdated actions. Dependabot bumps the SHA and its trailing
  # version comment together.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Asia/Manila
    open-pull-requests-limit: 5
    commit-message:
      prefix: ci
      include: scope
    groups:
      actions-minor-and-patch:
        applies-to: version-updates
        patterns: ["*"]
        update-types: [minor, patch]

  # Base images are pinned by digest in the Dockerfile, which has the same
  # decay problem as pinned actions: without an updater, pinning freezes the
  # runtime on an image that stops receiving patches. Dependabot rewrites the
  # `image:tag@sha256:...` reference in place, so the tag and the digest it
  # resolves to stay in step.
  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Asia/Manila
    open-pull-requests-limit: 5
    commit-message:
      prefix: ci
      include: scope
    groups:
      docker-minor-and-patch:
        applies-to: version-updates
        patterns: ["*"]
        update-types: [minor, patch]
